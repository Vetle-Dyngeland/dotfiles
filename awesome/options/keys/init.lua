return {
    mod = require("options.keys.mod"),
    global = require("options.keys.global"),
    client = require("options.keys.client"),
    clientbuttons = require("options.keys.clientbuttons"),
    widgetbuttons = require("options.keys.widgetbuttons")
}

return {
    mod = require("keys.mod"),
    global = require("keys.global"),
    client = require("keys.client"),
    clientbuttons = require("keys.clientbuttons"),
    widgetbuttons = require("keys.widgetbuttons")
}
